import numpy as np


def get_dataset_v3(N: int, K: float, u_bar: float, R: float) -> tuple[np.ndarray, np.ndarray]:
    "Half of the data sample randomly everywhere, half sample centred on the circle"
    pass
